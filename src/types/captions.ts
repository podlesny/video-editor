export type Caption = {
  alignment?: string,
  direction?: string,
  endTime: number,
  id: string,
  lineAlign?: string,
  linePosition?: string,
  pauseOnExit?: boolean,
  positionAlign?: string,
  size?: number,
  snapToLines?: boolean,
  startTime: number,
  text: string,
  textPosition?: string
}